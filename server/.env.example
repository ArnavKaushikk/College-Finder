PORT=5000
CLIENT_URL=http://localhost:3000
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/collegehub?retryWrites=true&w=majority
JWT_SECRET=your-long-random-secret-at-least-32-chars

# Copy to .env or .env.local (either works for seed + server)
