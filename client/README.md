# CollegeHub Client

Next.js frontend. All `/api/*` requests are proxied to the Express server (see `next.config.mjs`).

```bash
cp .env.example .env.local
npm install
npm run dev
```

Requires the API server running (see `../server/README.md`).
